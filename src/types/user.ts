// [파일 역할] 사용자 한 명에게서 화면과 Query 저장소가 실제로 사용할 세 값만 정합니다.
// [문법] `type`은 TypeScript가 코드를 검사할 때만 쓰이고 앱 실행 중에는 존재하지 않습니다.
// 따라서 외부 JSON의 실제 값 검사는 Zod가 따로 맡습니다.

// ======================================= 사용자 type 계약 ========================================

export type User = {
  // API가 더 많은 값을 보내도 앱에서 필요한 id, name, email만 User에 담습니다.
  id: number;
  name: string;
  email: string;
};

// =================================================================================================
